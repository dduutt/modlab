# Settings Modal Enhancements Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lock Role selection and convert Serial Port to dropdown select in `SettingsModal.vue`.

**Architecture:** Update `src/components/SettingsModal.vue`.

**Tech Stack:** Vue 3, TypeScript, pnpm.

## Global Constraints
- Use `pnpm` for build verification.
- Keep existing props and emits intact.

---

### Task 1: Update `SettingsModal.vue` for Role Locking and Serial Port Dropdown

**Files:**
- Modify: `src/components/SettingsModal.vue`

- [ ] **Step 1: Update template for disabled Role buttons and Serial Port select dropdown**

- [ ] **Step 2: Run verification with pnpm run build**
